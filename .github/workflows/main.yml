name: Lint
on: [push]

jobs:

  lint:
    name: Lint
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@master
      
    - name: Run ESLint
      uses: stefanoeb/eslint-action@master
